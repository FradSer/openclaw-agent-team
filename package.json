{
  "name": "openclaw-agent-team-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo for OpenClaw Agent Team plugin",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  },
  "packageManager": "pnpm@10.5.2",
  "engines": {
    "node": ">=20.0.0"
  }
}
